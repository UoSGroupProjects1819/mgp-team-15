using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanBeRotated : MonoBehaviour
{
    //Tells builder if the object can be rotated 45 degrees, currently used for slopes
    public bool canBeRotated = false;
}
